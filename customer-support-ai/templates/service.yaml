apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    {{- include "customer-support-ai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app : backend
 